# 阻碍清单（转交处理）

> 生成：2026-09-18，由 ZCode 会话整理。这些阻碍造成了近期迭代效率下降与两次错误部署。
> 按"阻碍 → 证据 → 要求整改项"组织，处理完请逐条勾掉。

## A. 并发写入冲突（最高优先级——已造成 3 次事故）

**现象/证据**（均发生在 `projects/theme-01/study/shader-se-clone/index.html`）：
1. 我的写入被外部旧缓冲覆盖至少 3 次（文件指纹比对发现：`__lenis` 诊断出口加入后消失；版本内容在两次读取之间漂移）；
2. 一次"文档拼接"事故：整份 HTML 被复制追加到文件尾（2288 行→2574 行，第二份文档嵌进了着色器字符串）；
3. v12 部署时关键修复（scroll 自反馈守卫）被覆盖丢失，导致用户再次报"滚动失效"，返工一轮。

**整改要求**：
- [ ] 同一时间只允许一个 agent 持有本工作区写权限。ZCode 会话进行中，其他 agent/工具不得写 `/Users/iiiiiei/Desktop/iiiiiei.com/` 下任何文件（包括批量格式化、压缩、重排、"修复"）。
- [ ] 若执行过批量操作（如图片压缩），列出全部改动过的路径。注：本仓库 git 已审计干净（无图片被改、无未提交变更）；若操作发生在 `~/.zcode` 或会话工件目录，请同样冻结。
- [ ] 说明工作区里 `.zcode/` 目录（2026-09-15 创建）与 0 字节根 `README.md`（2026-09-16）的来源；非必要请移除。
- [ ] 若未来需要多 agent 协作：约定"写前必读当前内容 + 写后指纹校验"，或以 git 分支隔离。

## B. 验证环境节流（效率下降主因之二）

**现象/证据**：内置浏览器面板被遮挡/后台时，requestAnimationFrame 停发——about:blank 实测 0 帧/1.5s，业务页帧数卡在 1。导致我**无法动态验证**滚动/动画/鼠标交互，只能静态截图，进而依赖用户人工验收，形成"部署→你发现问题→返工"循环。

**整改要求（用户侧）**：
- [ ] 我跑验证的那几分钟内：ZCode 窗口与右侧浏览器面板保持前台可见、系统不休眠、不最小化。
- [ ] 若仍被节流，告知我，我将切换为"页面内日志探针"验证方案（不依赖连续帧）。

## C. 验收通道缓存（假象制造者）

**现象**：GitHub Pages CDN 缓存 10 分钟，用户刷新可能看到旧版，造成"修了但没生效"的假象（模型错乱的一次复报疑似此因）。

**整改要求（用户侧）**：
- [ ] 验收一律用带参数链接（如 `https://iiiiiei.github.io/pages01/?v=15`）；
- [ ] 或浏览器 DevTools → Network → Disable cache。

## D. ZCode 会话自身的已知坑（无需整改，备案）

- 截图工件过大曾卡死会话（已约定：压缩+限张数）；
- node_repl 每次调用需重新 bootstrap 浏览器、连拍偶发失败重试；
- 我方已自我整改：所有文件写入改为**绝对路径+原子写入+写后指纹校验**（v12 事故的另一半根因是我用了相对路径写错文件，此责任在我）。
